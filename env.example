# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and modify as needed for your local setup

# =============================================================================
# SERVICE PORTS (DO NOT CHANGE UNLESS NECESSARY)
# =============================================================================
BACKEND_PORT=5055
FLUTTER_WEB_PORT=8080
POSTGRES_PORT=5432
REDIS_PORT=6379

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
# Flask Backend Settings
PORT=${BACKEND_PORT}
SECRET_KEY=dev-secret-key-change-in-production
ENVIRONMENT=local

# Database Configuration
DATABASE_URL=postgresql+psycopg://ai_buddy:ai_buddy_password@localhost:${POSTGRES_PORT}/mental_health
POSTGRES_DB=mental_health
POSTGRES_USER=ai_buddy
POSTGRES_PASSWORD=ai_buddy_password

# Redis Configuration (optional for local development)
REDIS_URL=redis://localhost:${REDIS_PORT}

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================
AI_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
PPLX_API_KEY=your_perplexity_api_key_here

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Flutter Web Configuration
FLUTTER_WEB_PORT=${FLUTTER_WEB_PORT}
API_URL=http://localhost:${BACKEND_PORT}

# =============================================================================
# PRODUCTION OVERRIDES
# =============================================================================
# These will be overridden in production environments
# PRODUCTION_API_URL=https://ai-mental-health-assistant-tddc.onrender.com 