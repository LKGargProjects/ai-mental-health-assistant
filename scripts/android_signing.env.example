# Example env file for Android signing. Copy to scripts/android_signing.env and fill real values.
# This file is NOT used automatically unless the release scripts source it.
# DO NOT COMMIT the real scripts/android_signing.env file.

# Absolute or project-relative path to your keystore (recommended location shown below)
STORE_FILE=ai_buddy_web/android/app/upload-keystore.jks

# Keystore password
STORE_PASSWORD=changeit

# Key alias inside the keystore
KEY_ALIAS=upload

# Key password for the alias
KEY_PASSWORD=changeit
