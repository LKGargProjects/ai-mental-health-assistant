# iOS signing env (example). Copy to scripts/ios_signing.env and fill values.
# This file is gitignored when named ios_signing.env.

# Export method for IPA: app-store | ad-hoc | development | enterprise
IOS_EXPORT_METHOD=ad-hoc

# Your Apple Team ID (e.g., ABCDE12345). Used when export options specify automatic signing.
APPLE_TEAM_ID=

# Optional explicit provisioning mapping for automatic export when multiple profiles exist
# Set both if you want to pin a specific provisioning profile for the main app target
IOS_BUNDLE_ID=app.mhb.preview
IOS_PROVISIONING_PROFILE_SPECIFIER=

# Notes:
# - For local builds, ensure Xcode is installed and you are logged into your Apple account in Xcode.
# - Automatic signing requires a valid signing certificate and a provisioning profile available to Xcode.
# - If automatic signing fails, set IOS_BUNDLE_ID and IOS_PROVISIONING_PROFILE_SPECIFIER to a provisioning profile name.
